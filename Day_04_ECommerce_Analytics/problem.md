
# Day 4: E-Commerce Sales Performance (Aggregations & Rankings)

## Objectives
* Sort and constrain analytical pipelines via `ORDER BY` and `LIMIT`.
* Run system statistics computations (`COUNT`, `SUM`, `AVG`, `MAX`, `MIN`).
* Filter structural matrix aggregations using `GROUP BY` and `HAVING`.
* Execute data windowing techniques via analytical `RANK() OVER()`.
