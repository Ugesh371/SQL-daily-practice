# Day 2: Bookstore Inventory (Constraints & Modifiers)

## Objectives
* Implement relational integrity using `PRIMARY KEY` and `FOREIGN KEY`.
* Enforce business rules using `CHECK` and `UNIQUE` constraints.
* Practice basic data modification (`UPDATE`, `DELETE`, `TRUNCATE`).
