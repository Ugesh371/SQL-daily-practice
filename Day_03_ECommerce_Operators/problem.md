
# Day 3: E-Commerce Platform (Conditional Filtering & Operators)

## Objectives
* Leverage filtering operators (`AND`, `OR`, `NOT`).
* Query data ranges and pattern matching via `BETWEEN`, `IN`, and `LIKE`.
* Manage calculated selections using arithmetic operators and column aliasing (`AS`).
