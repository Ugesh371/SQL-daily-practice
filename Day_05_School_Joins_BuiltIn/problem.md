
# Day 5: Student Enrollment Metrics (Complex Joins & Built-in Functions)

## Objectives
* Connect related data using `INNER JOIN`, `LEFT JOIN`, and `RIGHT JOIN`.
* Apply arithmetic built-in tools (`ROUND`, `ABS`, `MOD`).
* Master text cleaning operations via `CONCAT`, `LENGTH`, `REPLACE`, `SUBSTRING`, `UPPER`, and `LOWER`.
* Manage temporal data fields using tracking expressions (`NOW`, `DATEDIFF`, `DATE_ADD`).
